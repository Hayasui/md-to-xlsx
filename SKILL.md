---
name: md-to-research-xlsx
description: 把用研 Markdown 文档（用研需求、玩家甄别问卷、满意度或偏好类问卷、访谈大纲，含设计过程稿）转换成中英对照或纯中文的 Excel 交付物。当用户提供用研 md 文件并要求生成 xlsx、或提到「转成 Excel 留档」「生成用研表格」「给需求方看的表」时使用。适用于游戏/产品用研场景下基于 UserTesting 等平台的测试材料整理。
agent_created: true
---

# 用研 Markdown → Excel

把用研项目的 Markdown 文档（需求、甄别问卷、访谈大纲、满意度或偏好类问卷）转成一份
中英对照或纯中文的 Excel 交付物，用于上传在线文档留档、与需求方沟通。

## 表的组成（三类，实例数自由）

交付物由三大类表构成，**每类可放任意多个实例，也可以一个都不放**：

| 类别 | 说明 | 通常出现 |
|---|---|---|
| 需求梳理 | 纯中文 | 1 份 |
| 问卷 | 中英逐行并列（`survey`）或纯中文（`survey_cn`） | 1 份（也可能 2 份以上） |
| 访谈大纲 | 中英逐行并列 | 0-2 份 |

常见的组合：

- **三表**：需求梳理 · 玩家筛选问卷 · 无主持人访谈大纲
- **四表**：再加一份「有主持人访谈大纲」
- **只要问卷**：某项目不出大纲，甚至可能出两份不同用途的问卷
- **纯中文问卷**：中文定稿在先、英文版尚未制作，先用 `survey_cn` 出一版

不要假设组合是固定的。老脚本按 `SHEET1/2/3/4` 写死，遇到"两份问卷但不出大纲"
就得改代码；现在是往 `META["sheets"]` 里加减条目即可。

**问卷有两种列规格**，靠 `type` 区分：

| type | 列 | 用在 |
|---|---|---|
| `survey` | 模块 / 题型 / 题号 / 中文 / English / 问卷逻辑 | 甄别问卷，以及已经出好英文版的任何问卷 |
| `survey_cn` | 模块 / 题型 / 题号 / 中文 / 问卷逻辑 | 中文已定稿、英文尚未制作的问卷 |

两者共用同一个构建函数，行格式只差英文那一格。中文稿先出纯中文版、本地化完成后再
换成 `survey`，是这个仓库支持的正常路径，不是权宜之计。

### 最左列是「目的」（2026-09-11 起）

问卷表的第一列写这道题要拿什么数，按题纵向合并。它不是玩家可见内容，是给读表人
（需求方、配置问卷的人）用的导航：扫一眼就知道这道题在研究里干什么。写法照大纲表
的「目的」列——平实中文的短句，「了解玩家在什么场景下玩游戏」比「场景画像」好读。

**模块不进这一列**，`section` 行出的**分节横条**管模块，横条下面第一条就是这一组的
第一道题。两处都写模块名，同一个名字会出现两遍——这件事返工过一次，别再翻回去。

`prose` 行可以带第三个元素，作为「目的」格里的标签（如「开场说明」「板块引导语」
「结束语」），不然引导语那一行会剩一个空白的「目的」格，看不出是干什么的。

**一个 `section` 行都没有时，生成脚本直接报错**（`check_modules`），并把全部题干列
出来。别自己随手编几个模块就往下走，按这两步跟用户确认：

一是切分。按题目性质分组，每组起一个功能性的名字。常见的切法有基础信息收集
（人口统计、设备与场景）、品类认知与参与（玩过什么、在玩什么、投入多久）、
态度与偏好（吸引力、满意度、卖点排序）、付费与社交、流失与回流、触达与内容消费。
**md 里已有的段落名能直接用就沿用**，这是首选；只有当段落名是「第一部分」这类不含
信息的写法、或者一段里混了两种性质时，才需要重切。

二是确认。把「模块名 + 它覆盖的题号区间」摆给用户过一眼，他点头之后再写进内容区。
模块名只出现在分节横条上，但它是读表的分水岭，取功能性的说法：
「付费与留存」比「第三部分」有用。带「一、二、」这类序号没问题，序号能看出顺序。

### 题号列上的选项序号

题号列里，题目首行是 `Qn`，选项行上写该题的选项序号（1、2、3……）。读表的人扫一眼
这一列就知道这道题有几个选项、第几项写了什么。因此**题号列不做纵向合并**——合并了
就只剩首行有值，序号写不进去。目的与题型两列仍按题合并。

**两张问卷表和访谈大纲都走这个规则。** 矩阵题的陈述（`opt` 行）照常编号，七条陈述
就是 1-7，不必特殊处理。不编号的只有两类：`scale` 行是量表标签
（「1 = 很不同意 ｜ 5 = 很同意」），它是图例不是选项；`field` 行是选择题下面
附带的补充输入，也不是选项。有主持人版常常一个 `opt` 行都没有——选项由主持人
念，档位改写进「观察记录点」——那一列自然就是空的。

**历史踩坑（2026-09-11）**：这条规则写进了 spec，两个问卷构建函数
（`build_survey` / `build_survey_cn`）也照做了，**但 `build_outline` 漏了**。
大纲的题号列一直按题纵向合并，选项行全是空值，交付物上「第几项写了什么」无从对照；
演示的方式是给 Q19 的七条陈述标上 1-7。改法是两处：`close_block()` 从
「目的／题号／题型」三列缩成两列，`opt` 行在题号列写自增序号。

**四栏齐头（2026-09-14）**：大纲的 `fu` / `obs` 不各占一行从题干下面起头，
而是**每条题干行的第一条追问与第一条记录点落在题干行本身**，四栏从同一行开始；
第二条起各自另起一行。`kv` 行下的记录点同样办理。`scale` 行是图例，不作为附着点。
这一条是为了消掉「追问与记录点比中文/English 低一行」的错位。

`opt` 行因此有两副面孔：无主持人版里它是选项（题号列写自增序号）；
有主持人版里它可以充当**子问**，在行末补第四个元素写死标签
（`("opt", 中文, English, "19-2")`），用来承载被拆开的矩阵题——如 Q19 由 1-5 矩阵
改为 19-1/2/3 三问定性时，三条子问各带自己的追问。
细节见 `references/format-spec.md`。

**表名跟随问卷用途**，不要固定写「玩家筛选问卷」：甄别问卷叫「玩家筛选问卷」，
满意度或偏好类问卷叫「题材偏好问卷」「满意度问卷」等，按项目实际用途命名。

**不要把非甄别类问卷硬塞进甄别问卷的框子里。** 甄别问卷有筛选逻辑（通过 / 终止），
而满意度问卷没有——如果沿用「判定」这个列名，需求方会追问"这一列怎么全是空的"。
统一用「问卷逻辑」的原因见下。

**两版大纲不要合并成一张表**：无主持人版把追问写进题干，有主持人版由主持人现场提问，
两者承载方式不同，合并会丢掉这个差别，也没法并排对照。

**大纲的可选两列**：有主持人版通常要放「追问方向 / 观察记录点」，即八列版
（`COLUMNS_8`）；无主持人版一般六列。两版的 模块 / 目的 / 题号 / 题型 / 中文 / English
六列是同构的，题号一一对应，靠这六列就能并排比较。
**不建议给六列版补两列空列**——需求方看到空列会追问"这两列为什么是空的"。

观察记录点本属内部工作内容，按删减清单该删；但**用户明确要求保留时要照做**，
不要自作主张删掉——先按规范建议、再听用户裁决。

**两版「目的」列必须逐条一致**：同一道题在两版里要写同一个词。
历史踩坑：一版抄上一版的写法（"了解玩家的常用设备"），另一版直接抄 md 的 `#### 标题`
（"平时用什么设备玩休闲游戏"），同一件事两种写法，需求方一眼就能看出来。
生成后跑一次逐题比对。

## 「问卷逻辑」这一列

问卷的末列统一叫「问卷逻辑」，记录题目之间的跳转与因果关系。它与流程图上箭头
表达的是同一件事，落到表里就是「选了这一项之后会发生什么」。

列名统一，语义由每份表自己的内容决定：

| 场景 | 逻辑列的写法 |
|---|---|
| 甄别问卷的硬门槛 | `终止`（红色加粗），照 md 写，不改写措辞 |
| 满意度问卷跳过整个板块 | `跳转到 Q6` |
| 最后一个板块跳过 | `本模块结束` |
| 互斥选项 | `与其余色调选项互斥，不可同时选中` |
| 选项触出文本框 | `需填写文本（必填，≤20 字符）` |

两条写法上的规矩：

- **整题门槛写在题号行的逻辑格里，选项跳转写在选项行上。** 两者同在一列，读者一眼分得出「谁要答这道题」与「选了这项之后去哪」。
- **逻辑记在触发它的那一行**，不要写在被依赖的题目上。历史踩坑：早先把
  「Q4 未勾选跳过项」写在 Q5 的格子里，读者要反过来推导是哪条路径导致的隐藏，
  配置问卷时也容易漏；改成在 Q4 的跳过选项旁写「跳转到 Q6」，一处就交代清楚了。
- **依赖型题目不写条件**。吸引点题之所以出现，是因为玩家上一步没选跳过，
  这层关系由上一条跳转隐含，不必重复。
- 数目标注（「最多选 3 项」）仍写在题干里，它约束的是同题内的选择数量，
  与选项触发的行为不是一回事。

**填写问卷的人看不到这一列**。它是给配置问卷的人看的，不需要为了玩家可读而改写措辞。

### 「选择题 + 附加文本输入」的平台前提

满意度类问卷常出现一种题：一道多选题下面跟一个文本框（「用一句话说说为什么」）。
在表里它是一行 `field`，与上一题共用题型与题号——**前提是问卷平台支持在选择题下
附加文本输入**。

若平台只能把选择题与填空拆成两道题，题号就要整体重排：
一道「多选 + 补充文本」变成两道，全卷题数随之增加。这件事**在生成交付表之前就要问清**，
否则需求方拿到的表与平台上的实际题号对不上。

拿不准时按一种写，并在汇报里说明这个前提，以及不成立时题号会怎么变。

## 核心目的（决定一切取舍）

**这份表是给需求方（运营 / 产品）看的，不是给用研内部看的。**

所以只有一条总原则：**写得简洁易懂，非必要呈现在文档里的文字一律删掉。**

具体地说，需求方要看到的是"要测什么、怎么筛人、问什么问题"；
他们不需要知道"我们在哪个平台做、用什么形式录、怎么配置"。

md 是内部工作稿，可以写得很满（研究落点、内部备注、平台限制、配置说明）；
xlsx 是交付物，必须把这一层全部剥掉。**md 里的内容不等于 xlsx 里的内容**——
每次转换都要主动做一次"站在需求方视角"的删减。

**需求梳理表的删减尺度宽于问卷表**，这一点要分开处理。问卷表里每一行都必须是
玩家在平台上真正看到的东西，字符级地严；需求表通篇都在讲研究设计——
研究目标、假设、样本条件、分析口径——这些本来就是要给需求方看的。
判据是**这份需求表给谁**：

- 给需求方（运营 / 产品）→ 只留他们要判断的事，分析方法的细节可省
- 给组内同事 → 可以保留分析口径（样本量阈值、编码一致性要求等）

拿不准就问一句，不要按默认尺度一刀切。

## 何时使用

用户的工作流是：**用 Markdown 撰写和迭代问卷/访谈大纲（便于自己和 AI 修改）→
让 AI 把 md 转成 xlsx 留档**。当出现以下情况时使用本 skill：

- 用户给出用研相关的 md 文件，要求生成 Excel
- 用户提到「转成 Excel」「生成表格留档」「给需求方看的版本」
- 用户要求整理用研需求、甄别问卷、访谈大纲成表格

三张表（用研需求简述、玩家筛选问卷、访谈大纲）是标准配置。

## 工作流程

### 第 1 步：读取源文件

先读用户指定的 md 文件。通常成对出现：

- `xxx甄别问卷.md` + `xxx甄别问卷_EN.md`
- `xxx访谈大纲.md` + `xxx访谈大纲_EN.md`
- 可能还有独立的 `需求.md`（若有，优先用它作为表格 1 的内容来源）

**输入不一定是成品稿。** 真实项目里更常见的是**设计过程稿**：一份还在迭代的
文档，里面有结构总览、方案对比、设计理由、编码流程、桌面研究附录，真正的问卷
只占其中一节。读的时候要先判断手上是哪一种，再决定怎么取。

若确实是过程稿，**只把最终定的那版问卷取出来**，以下内容一律留在 md 里：

| 不进表 | 例 |
|---|---|
| 结构与流程总览 | 题目之间的导航图、问卷结构树 |
| 方案对比与取舍 | 方案 A / B / C 的优劣分析、为什么删掉某个选项 |
| 设计理由与论证 | "这一题的价值落在四个地方"、标签池结构的说明 |
| 编码与分析方案 | 抽样框架、剔除规则、Kappa 要求、交付物模板 |
| 桌面研究附录 | 题材热度调研、来源链接 |
| 待确认标记 | 「待确认」「待定」「需与需求方对齐」 |
| 版本变更记录 | v0.1 → v0.7 的修改史 |

一条判断标准：**这句话是玩家会看到的、需求方要判断的，还是设计者自己要想清楚的**。
第三类全部留在 md。取完内容后，把「这份 md 里哪些进了表、哪些没进」列给用户过一眼，
过程稿尤其要做这一步——漏取一节和少删一段都看不出来。

### 第 1·5 步：能跑结构比对就先跑

中英两版都在手边时，先跑结构比对，快速找出两边题数/选项数/量表端点不一致的地方：

```bash
python ../sc-to-en/scripts/lint_usertesting.py <英文版.md> --sc <中文版.md>
```

注意：脚本只查**结构**（题号、选项数、量表端点），文案措辞差异仍需逐条人工核对。

**中英不一致时怎么办**：中文 md 是**源头**，英文 md 是它的**本地化产物**。脚本只负责**报出来**，不自行裁决——由用户判断是"英文漏了"还是"中文后来改了英文没跟"。

**只有中文稿时跳过这一步。** 中文定稿在先、英文尚未制作，是这个流程的正常状态，
不是例外；这种情况下问卷表用 `survey_cn` 出纯中文版，等英文做完了再换回 `survey`。

> 中英结构比对由 `sc-to-en` 的 lint 统一负责，本 skill 不再自带比对脚本（避免两套逻辑漂移）。

### 第 2 步：确认无法从 md 推断的信息

只问少数几个必须问的问题，**不要重新讨论已固化的结构规范**（列定义、样式、
中英排布见 `references/format-spec.md`，已定死）：

- **项目编号**：`【YYMM】` 格式，YYMM 为当前年月。先扫目录里已有的 `【NNNN】` 文件，
  默认取最大值 +1 作为候选，请用户确认
  （历史踩坑：某项目用过 `【K3P】` 这类非日期编号，所以候选值要请用户点头，不要直接采用）
- **项目简称**：用于文件名
- **问卷用途**：决定问卷表的表名（玩家筛选问卷 / 题材偏好问卷 / 满意度问卷……）
- **是否出英文版**：决定用 `survey` 还是 `survey_cn`
- **访谈形式**：决定大纲表的表名（无主持人访谈大纲 / 有主持人访谈大纲）
- **md 里缺失的字段**：如样本量、素材链接、上线时间等，找不到就问，或先填「待定」
- **需求表内容来源**：有独立 `需求.md` 就用它；否则从访谈大纲的研究问题
  和甄别问卷的样本条件里提炼，并把提炼不出的列出来问
- **需求表的读者**：给需求方还是给组内同事，决定分析口径一层留不留

**玩家可见的引导语，md 里没有就索取，不要代拟。** 开场说明、板块引导语
这类文字玩家会真真切切读到，语气和措辞归用户定。历史踩坑：某项目 md 里
只写了"每节开头给出引导语"，实施时代拟了两句，用户看到后才发现不是自己想说的。

### 第 3 步：填充脚本内容区

复制两个文件到工作目录：`scripts/build_research_xlsx.py` **和 `scripts/validate_xlsx.py`**。
后者漏了不会中断——脚本会先到技能安装目录去找，两处都没有才跳过校验，并明确提示；
看到那条提示就把文件复制过去重跑，**「已生成」不等于校验通过**。

项目自己的生成脚本（`_build_<项目>.py`）**结尾交给技能里的 `main()` 跑**，别自己手写一遍
`wb.save()`：手写会绕过自动备份与自动校验，而「已生成」照常打印，看上去像一切正常。
写法是把内容区的 `META` 挂钩过去再调用：

```python
if __name__ == "__main__":
    sys.argv = [sys.argv[0]]   # 输出路径固定取 META["filename"]
    B.META = META
    B.main()                   # 备份已有文件 → 按 META 生成 → 跑 validate_xlsx
```

`META` 挂过去之前先把 `sys.argv` 收干净。`main()` 会把 argv 里第一个非开关参数当输出文件名，
若上层用 `runpy` 之类的壳子调用，参数里带着壳子脚本名，就会生成一个名叫那个脚本的文件，
且不报错。

脚本按**三类**组织，每类可放任意多个实例（含 0 个）：

| 类型 `type` | 类别 | 语言 | 常用列数 |
|---|---|---|---|
| `needs` | 需求梳理 | 纯中文 | 3 |
| `survey` | 问卷（中英并列） | 中英逐行并列 | 5 |
| `survey_cn` | 问卷（纯中文） | 纯中文 | 4 |
| `outline` | 访谈大纲 | 中英逐行并列 | 6 或 8 |

修改「内容区」时：

- `NEEDS`：用 `kv` / `kvlist` / `numlist` / `muted` 四种类型
- `SURVEY`：用 `section` / `prose` / `q` 三种类型。
  它的 `q` 行是 `("q", 题型, 题号, 中文题干, 英文题干, 问卷逻辑, 选项集, "目的")`，
  末尾那个「目的」与 `SURVEY_CN` 一样写进最左列、按题纵向合并。
  **两种问卷的 `q` 行都靠索引取值**，元素个数可变——写定长解包的话，
  带「目的」的行会当场抛 `too many values to unpack`
- `SURVEY_CN`：用 `section` / `prose` / `q` / `field` 四种类型。
  `section` 行出分节横条，一份问卷至少要有一个 `section` 行，一个都没有时
  脚本直接报错（见上文「最左列是目的」一节）；`q` 行最后一个元素是这道题的
  「目的」，写进最左列，写法见同一节
  `field` 是并列在选择题下的补充输入行（如「用一句话说说为什么」），
  与上一题共用题型与题号；它的存在前提是平台支持"选择题 + 附加文本"，
  不支持时要另立题号，参考 `references/format-spec.md`
- `q` 行的第六个元素可选，写**整题的问卷逻辑**：答题门槛、整题跳转、「全卷适用」
  这类不挂在某一个选项上的规则（例：`("q", "多选", "Q5", 题干, 选项, "仅 Q2 勾选「MMORPG」者作答")`）。
  不写就留空，逻辑只落在触发它的选项行上
- `OUTLINE_*`：用 `page` / `kv` / `sub` / `q` / `scale` / `opt` 六种类型；
  若该版大纲要放追问与观察记录点，再加 `fu` / `obs`
- `META["sheets"]`：**列表顺序就是工作表顺序**，这决定了文档里先出现哪张表

```python
META = {
    "filename": "【YYMM】项目名.xlsx",
    "sheets": [
        {"type": "needs",     "name": "需求梳理",     "content": NEEDS},
        {"type": "survey",    "name": "玩家筛选问卷", "content": SURVEY},
        {"type": "outline",   "name": "无主持人访谈大纲", "content": OUTLINE_UNMODERATED},
        {"type": "outline",   "name": "有主持人访谈大纲", "content": OUTLINE_MODERATED,
         "columns": COLUMNS_8},
        # {"type": "survey_cn", "name": "题材偏好问卷", "content": SURVEY_CN},
    ],
}
```

**实例数完全自由**，不要假设必然是"一需求一问卷一大纲"。
真实项目里出现过"两份问卷但不出大纲""只出问卷不出大纲"这类组合，
直接往 `sheets` 里加减条目即可。

**"不需要改代码"只对实例数成立，对新增表类型不成立。**
若是已知类型换份数，改 `META["sheets"]` 就够了；若这个项目的表结构是全新的
（比如一种既非甄别、也非纯中文的问卷），要在脚本里加四处：

1. `COLUMNS_*`：声明列名
2. `WIDTHS`：声明列宽（大纲按列数取预设，问卷按类型取）
3. `TAB_COLORS`：声明标签页颜色
4. 新增一个 `build_xxx()` 函数，并在 `main()` 的分发里加上分支

改完先跑一次生成，确认表清单、行数、列数与预期一致，再往下走。

**大纲的列显式声明**，默认 `COLUMNS_6`（模块 / 目的 / 题号 / 题型 / 中文 / English）；
要在后面追加「追问方向」「观察记录点」两列，就把该实例写成 `"columns": COLUMNS_8`。
之所以不自动推断，是因为自动推断时"某版恰好没写 `fu`/`obs` 行"会导致这两列静默消失、
表头与列宽跟着变，却不报错。显式声明则会在用了 `fu` 行却没声明列时当场报错：

```
ValueError: 大纲「测试大纲」用了 "obs" 行，但 columns 里没有「观察记录点」列。
```

**可选字段**：`"widths": {4: 10, 5: 40, 6: 40}` 覆盖个别列宽（键为 0 起的列序），
`"tab_color": "1F3A4D"` 指定标签页颜色（不写就按类型取，大纲多实例自动顺延换色）。

各类型的字段含义写在脚本注释里，完整可跑的实战样例见
`references/example-4sheets.py`（四张表的真实项目脚本）。**样式区不要改**，它是固化规范。

**`prose` 的两种写法**：`("prose", [(中, 英), ...])` 每个元组占一行；
若要把整段文字放进**同一个单元格**（如结束语的五个自然段），
就只写一个元组、在字符串里用 `\n` 换行：

```python
("prose", [("第一段。\n第二段。\n第三段。", "Para 1.\nPara 2.\nPara 3.")]),
```

**大纲的题号写 `Q1` 式，不写「问题 N」**。中文 md 正文用「问题 N」是内部口径，
xlsx 交付物统一 `Q`（与问卷表一致）。历史踩坑：29 处「问题 N」要全量替换。

**档位题要写全档位**：主持人版的口头作答题（时长、付费、下载意愿）不念选项，
但观察记录点里必须写明档位清单，否则记录时没有归档依据。

### 插入新题 / 顺移题号（高频操作，易出错）

问卷改稿常要"在 Q2 后插一题、后面整体 +1"。做这件事时：

1. **改前先备份**（`_backup/md_<日期>/`），md 不在版本控制里时尤其要紧。
2. **不要只按「题号前缀」做替换**。历史踩坑：中文问卷里「等级」和「连续玩二合」
   两题都曾是 `Q4`，用 `replace('#### Q4. ', '#### Q5. ')` 会命中错的那一个，
   结果两题都变成同一个号。**替换时要带上题干关键词**，例如
   `'#### Q4. 在《Gossip Harbor》里'` → `'#### Q5. 在《Gossip Harbor》里'`。
3. **替换完立刻列一遍全部 `#### Q` 行**核对，不要等生成完再发现。
4. **连带引用一起改**，这是最容易漏的一步：
   - 甄别问卷的「甄别逻辑总览」表（新增一行、其余整体 +1）
   - 甄别问卷附录（终止逻辑那条列了所有题号；「编号样式」那条写了区间与陷阱题号）
   - 甄别问卷首尾的「只负责资格核验（Q1-Qn）」、附录「保留 Q1-Qn 的判定标记」
   - **两份访谈大纲里的内部引用**（如「平台已定向确认 Android 设备（甄别问卷 Qn）」、
     「等级核验是甄别问卷 Qn 的延伸条件」）——这类藏在别文件里的引用最常被忘掉
5. 改完跑一次 lint，甄别问卷的中英结构差异会暴露出来。

**顺移后英文版要同步**：英文 md 的题号、总览表、附录同样要改；
lint 的「对齐」检查会报出中文有、英文没有的题号。

### 批量改 md 的两个机制坑（2026-09-11 实战）

用脚本一次改多份 md 时，有两处会让改动**静默失效**，都没报错，只有结果不对。

**行尾。** 这类 md 是 CRLF 行尾，脚本里的多行模式若按 `\n` 拼，**单行模式照常命中，
多行的全部失配**。失配本身不抛异常，若脚本把"找不到"顺手当成"可能已经改过"放过，
就成了无声漏改。做法是先 `\r\n → \n` 归一、匹配完按原样还原 CRLF。
回报也要分清楚：算出"新版本文本"的出现次数，才能把"已改过"和"真没匹配上"分开。

**引号。** 块级删除（删一整道题）里通常夹着中文引号，而源码里是全角 `""` 还是半角 `""`
肉眼难辨，写死在 old_string 里容易失配。改用 `re.subn(pat, "", text, flags=re.DOTALL)`，
以题干首行和下一节标题为锚，中间一律 `.*?`，避开引号形状的分歧。

顺带一条环境上的老问题：跑项目脚本时**别把中文路径当命令行参数传给 python**（偶发乱码），
也**别指望 PowerShell 抓到 python 的 stdout**。在纯 ASCII 目录放个壳子，
目标路径写在源码里，用 `runpy.run_path(target, run_name="__main__")` 执行，
再用 `contextlib.redirect_stdout` 把输出收进 UTF-8 文件。

### 用户手动改过 xlsx 之后怎么回写

用户可能在腾讯文档 / Excel / WPS 里直接改表格内容——措辞、编号、表头，
也可能只是调了行高。回写前**先找出他改了哪些格**，不要请他复述：

```bash
# 用当前脚本另生成一份到临时文件（--no-backup 避免污染 _backup/）
python _build_<项目>.py _tmp_orig.xlsx --no-backup
```

用 openpyxl 逐格比对用户版与脚本版，差异格就是他的改动。这比让他复述一遍可靠，
也能发现他没提到的改动（例如复制粘贴时多出来的重复段落）。
**把他改动过的每一处报出来确认**，并同步进脚本与 md——不同步的话，
下一次重新生成就会把他的修改覆盖掉。

顺带一眼能看出文件被什么工具动过：读 `docProps/app.xml` 的 `<Application>`，
`Tencent office` 是腾讯文档，`WPS` 是 WPS，`openpyxl` 是本脚本写的。
比对时以**内容**为准，样式差异（行高、字体被工具重算）不必追究。

**提取内容时的关键约束**：

- **面向需求方删减（最重要的一条，见下节「删减清单」）**：只保留需求方需要看到的内容
- 只提取**玩家实际看到的内容**。md 里的「研究落点」「内部备注」等内部内容**不进表**
  （问卷表的「问卷逻辑」列除外——那一列照 md 原样写）
- 访谈大纲的「目的」列从 md 的「研究落点」改写成平实中文，
  **去掉 RQ 编号和「【】」自造标签**
- 严禁把追问塞进题干单元格；一格里只放一句话
- **逻辑记在触发它的那一行**，不要写在被依赖的题目上（见上文「问卷逻辑」一节）

**问卷逻辑列照 md 写，不做语义改写。** 这是一个单纯的转换工具：md 里写「人工复核」就写
「人工复核」，写「终止」就写「终止」，**不要自行改写成"机筛"或别的措辞**，
也不要替用户判断哪条规则更合理。历史踩坑：md 写「人工复核」、xlsx 里被改成了
「终止（机筛）」，两边对不上，交付时反而说不清以哪份为准。
若确实认为 md 的写法不妥，**指出并请用户改 md**，让源头与产物一致。

### 删减清单（每次转换必做）

按"给需求方看"的原则，以下内容**从 xlsx 里删掉**：

| 删 | 例 |
|---|---|
| 平台与执行信息 | 平台名、测试形式（Unmoderated / Think-Out-Loud）、录屏录音方式、"无人主持"、Zoom / 共享屏幕说明 |
| 内部标注 | 「（本研究的核心品类）」「（硬性门槛题）」这类自造补充 |
| 不影响作答的括号 | 「手机（安卓 / 苹果）」→「手机」 |
| 标题行与说明行 | 「【编号】项目名 · 玩家筛选问卷（中英对照 · 留档版）」、页面说明行 |
| 研究落点 / 内部备注 | 一律不进表（表格 3 的「目的」列除外，且要改写成平实中文） |

以下内容**保留**：

- 玩家在平台上真正看到的内容（题干、选项、量表标签、页面引导语）
- 研究目标与研究假设（需求方要看研究要解决什么）
- 影响作答的括号（「（可多选）」「（单选）」）
- 样本条件、单场时长、目标国家
- 问卷表的「问卷逻辑」列（照 md 原样写）
- 需求梳理表里的研究方法、交付物、分析口径——前提是这份需求表要给需求方看

**删减清单只对问卷表与大纲表从严，需求梳理表松一档。** 问卷表的每一行都必须是
玩家在平台上真正看到的东西；需求表通篇讲的是研究设计，其中的分析口径一层
（样本量阈值、编码一致性要求）算不算"非必要文字"，取决于这份表给谁看。
给组内同事的可以留，给需求方的可以省。拿不准就问。

**一个例外要问用户**：若 md 里写着「本表为留档版，编号仅用于内部沟通」这类说明行，
是加是删由用户定——它曾是用户主动要求的，后来又被删掉，不同项目取舍不同。
默认按"删"处理，但不要自己拍板，报出来。

### 术语中文化（给需求方看，不照抄平台原名）

平台题型名是 UserTesting 的内部定义，需求方不需要认识。**xlsx 的题型列一律用中文简述**：

| 平台原名 | xlsx 里写 |
|---|---|
| Multiple Choice（单选） | 单选 |
| Multiple Choice（多选） | 多选 |
| Verbal response | 口头回答 |
| Matrix | 矩阵 |
| Ranking | 排序 |
| Rating scale / NPS | 打分 / NPS |

两条细则：

- **NPS 不翻译**——它是约定俗成的用法，写成「净推荐值」反而增加理解成本。
- **题型列不标区间**——写「打分」即可。区间由题干和量表标签承载
  （「1 分最低，10 分最高」），两处重复容易写岔。
  历史踩坑：md 里题型标 `Rating scale · 0-10`，题干却是「1 分最低」——
  实际是 1-10。区间只在一处写，就不会打架。

题号用 `Q1` 而不是「题 1」（更短，且与问卷表统一）。

**题号用连续数字，不用 `Q4a` / `Q4b` 这种分支写法。** 一道选择题下面附带一个
文本输入框，是**一道题**，不拆成两个题号；真要拆（平台不支持附加输入时），
就整体顺移。历史踩坑：`Q4a` / `Q4b` 的写法在交付表里读起来像题号没定稿，
需求方会问"a 和 b 是什么关系"。

**非必答写在题型列里**，不在题干末尾缀「（选填）」，也不单开一列。
写「简答（非必答）」即可，这样题型列的信息是完整的。

### 表名跟随问卷用途与访谈形式

表 1「需求梳理」固定。后面几类的名字跟随项目实际内容：

- 问卷：`玩家筛选问卷`（甄别）／`题材偏好问卷`、`满意度问卷`（满意度类）
- 大纲：`无主持人访谈大纲` / `有主持人访谈大纲`

不要固定写成「问卷」或「访谈大纲」，那样会丢掉用途与形式的信息。
md 里通常已写明，没有就问。

### 第 4 步：生成与校验

```bash
python build_research_xlsx.py "【YYMM】项目简称.xlsx"
```

脚本会自动完成三件事：

1. 若目标文件已存在，**自动备份**到 `_backup/`（用 `--no-backup` 可跳过）
2. 按 `META["sheets"]` 的顺序生成各张表
3. **自动校验**并打印问题清单，分两级：**错误**（行高裁切、合并重叠、
   Markdown 残留、字体、打印设置、题号缺失/重复、跳转目标不存在）必须修复；
   **提醒**（删减关键词命中、层级称呼混用、末板块写法）是疑似问题，逐条
   过目后由你裁决

关于第 3 条：校验靠 `import validate_xlsx`，脚本先在同目录找，找不到会自动
到技能安装目录去找，两处都没有才跳过——此时会明确打印"本次跳过自动校验"
以及后果，**不要忽略这段提示**，`已生成` 三个字不代表任何校验结论。

必须修复校验报出的所有错误；提醒逐条过目，确认是真问题的改掉，是误报的放行。
单独校验已有文件时：

```bash
python validate_xlsx.py <文件.xlsx>
```

**改完 md 重新生成时**，先确认识别出的工作表清单与预期一致（行数、列数），
再拿结果与上一版比对——这一步能挡住"新增一题后题号错位""某表少生成一张"这类问题。

### 第 5 步：汇报

向用户汇报时说明：

1. **各张表**的行数与列结构（三类可能有多个实例，逐张列出）
2. **中英不一致之处**（报出来由用户判断，见下）
3. 哪些字段是「待定」，需要用户补充
4. 源文件里发现的其他问题（错别字、翻译遗漏等）
5. **删减了哪些内容**（按删减清单剥掉的平台信息、内部标注等），让用户复核
6. 输入若是**设计过程稿**，列出哪些章节进了表、哪些留在 md 里

### 中英不一致怎么处理

中文 md 是源头，英文 md 是本地化产物，xlsx 是忠实渲染。
**xlsx 不参与版本判定**——中英不一致时只报出来，不自行裁决。

先跑结构比对：

```bash
python ../sc-to-en/scripts/lint_usertesting.py <英文版.md> --sc <中文版.md>
```

## 覆盖策略（重要）

用户可能在 WPS / 腾讯文档 / Excel 里手动改过已生成的 xlsx。
重新生成会覆盖掉这些调整，而且**不同工具带来的副作用不同**：

| 工具 | 副作用 |
|---|---|
| WPS 保存 | 重算行高（比脚本估算的更紧凑） |
| 腾讯文档另存 | 字体换成等线、丢失冻结窗格与打印设置、多出空列 |

- 生成前若目标文件已存在，**先备份**到 `_backup/`
- 若用户已手动改过，**优先保留用户版本**，只把改动同步到脚本和 md 源文件
- 用户的手动修改用 `v1`、`v2` 后缀命名，与生成的原件区分
- **样式基线不跟工具走**：无论中间被什么工具打开保存过，重新生成的样式
  一律回到脚本里的固化规范（微软雅黑 / Aptos、冻结、关网格线、打印设置）。
  用户的实际反馈是"原来那么窄读起来挤"才改列宽，而不是因为某个工具算出来的数字不同

## 用户已确立的规范摘要

详细版见 `references/format-spec.md`。核心几条：

- **文档目的是给需求方看**：简洁易懂，非必要的文字一律删
  （需求梳理表松一档，见「核心目的」一节的说明）
- 需求梳理纯中文；问卷中英逐行并列（`survey`）或纯中文（`survey_cn`）；访谈大纲中英逐行并列
- 选项每个单独一行，题型 / 题号纵向合并
- 保留题号（`Q1` 式，连续数字，不用 `Q4a` 分支写法），不设标题行与「留档版」说明行
- 问卷末列统一「问卷逻辑」，记录跳转与因果关系，记在触发它的那一行上
- 问卷表最左列是「目的」，写这道题要拿什么数（`q` 行最后一个元素），按题纵向合并；
  模块靠 `section` 行出的分节横条表达，不再进列
- md 里没分模块时，先跟用户确认模块名与它覆盖的题号区间，不要自己拍
- 题号列在选项行上写该题的选项序号（1、2、3……），这一列因此不做纵向合并
- 题号由脚本的 `q` 行统一带出，不靠人写「问题 N」
- 甄别问卷默认全部必答，不设「必答/选答」列；非必答写在题型列（`简答（非必答）`）
- 问卷逻辑列照 md 原样写，不自行改写措辞
- 题型列用中文简述（单选 / 多选 / 简答 / 填空 / 口头回答 / 矩阵 / 排序 / 打分 / NPS），不标区间
- 表名跟随文件用途：问卷按用途（玩家筛选问卷 / 题材偏好问卷 / 满意度问卷），
  大纲按形式（无主持人访谈大纲 / 有主持人访谈大纲）
- **字体**：中文列用微软雅黑，英文列用 Aptos，跨列合并的单元格用微软雅黑
- 样式：深色表头白字、细边框、关网格线、冻结首行（`A2`）、缩放 100%
- 打印：横向、缩放到一页宽、每页重复表头、页脚显示页码
- 沟通与产出都用流畅、有人味的中文

## QOL 功能

脚本已内置，无需额外配置：

| 功能 | 说明 |
|------|------|
| 自动备份 | 目标文件存在时自动存一份到 `_backup/`（用 `--no-backup` 跳过） |
| 自动校验 | 保存后立即校验，分两级：错误（裁切/重叠/残留/字体/打印/题号/跳转）必须修复，提醒（关键词/称呼/末板块写法）由人裁决；找不到校验脚本时明确提示，不静默跳过 |
| 字体分列 | 中文微软雅黑 / 英文 Aptos，跨列合并用中文 |
| 打印预设 | 横向、一页宽、重复表头、页码 |
| 行高自适应 | 按内容估算，避免裁切 |
| 区块分隔线 | 每道题首行加中粗上边框 |

## 环境注意事项

- 依赖 `openpyxl`
- 读取**他人制作或 WPS / 腾讯文档保存过**的 xlsx 时，列宽必须用 `zipfile` + `ElementTree`
  读原始 XML。openpyxl 的 `column_dimensions` 会漏掉被 WPS 合并定义的中间列
  （`<col min="3" max="4" width="54"/>` 形式）
- **判断一个 xlsx 被什么工具改过**：读 `docProps/app.xml` 的 `<Application>`。
  openpyxl 生成的是 `Microsoft Excel Compatible / Openpyxl x.x.x`；
  腾讯文档另存的是 `Tencent office`。这能解释字体、冻结、打印设置为何丢失
- PowerShell 的 COM 调用（`Ket.Application` / `Excel.Application`）在本环境被安全策略拦截，
  无法用 WPS 导 PDF 做视觉校验，改用 `validate_xlsx.py` 的字符宽度模型
- 行高校准：WPS 实测每行 15 磅，英文列每行约容纳「列宽 × 1.08」个字符。
  校验器的换算系数已按此定标，改列宽时需重新校准
- 脚本 `import` 校验器时走 `load_validator()`：先同目录，再技能安装目录
  （`~/.workbuddy/skills/md-to-research-xlsx/scripts`）。技能换位置要同步改这一处
- **别把中文路径当命令行参数传给 python**。本机（Windows）实测偶发：路径
  `C:\Agent\用研工作\...` 被按 GBK 解成乱码，脚本抛 `FileNotFoundError`，而报错信息里的
  路径也是乱的，容易被误判成文件真的不存在（同样的调用换个时机又能跑通，症状不稳定）。
  稳妥做法是在**纯 ASCII 目录**下放一个壳子脚本，目标路径在壳子源码里拼好（源码本身
  UTF-8），只从 `argv` 取 ASCII 的脚本名，再用 `runpy.run_path(target, run_name="__main__")`
  执行。壳子示例见 `scripts/run_project_script.py`
- **别指望 PowerShell 捕获 python 的 stdout**。实测 `& $py script.py` 常常什么都不返回
  或只回一个退出码，`*>` 重定向还会写成 UTF-16 而读不出来。要日志就让脚本自己写
  UTF-8 文件，或在壳子里用 `contextlib.redirect_stdout` 收进文件
- 改完 md 想确认 xlsx 是否被人手动动过，用 `scripts/check_drift.py`：加载项目的生成脚本、
  把 `META["filename"]` 换成临时路径、调它的 `main()` 重建一份，再逐格比对。
  所以项目脚本要把建表逻辑收进一个 `main()`（见第 3 步）

## 一致性检查（生成后人工过一遍）

脚本已能报三类：内部关键词命中（疑似该删未删）、层级称呼混用
（模块 / 板块 / 第X部分 / 章节 / 品类节）、末板块写法——都是 warning，
报出来仍要人过目。以下这些脚本查不出来，但需求方一眼能看见：

- 同一类标注在 A 处保留、B 处删掉（如「性别」去了「（由平台定向筛选）」而「年龄」还留着）
- 同一层级的模块名前缀不一致（如 `Instruction` 去了 `Task A · ` 而 `Navigation` 保留）
- 题型列中英混用（如「单选 / 矩阵」与 `Rating scale / NPS` 并存）
- 中英两侧题数、选项数、量表端点不一致
- **术语在全文里是否只有一种说法**：同一件事反复出现时，用词是否统一
  （「模块」「板块」这类高危近义词脚本已能报，近义词组之外的说法要靠人，
  如「步骤」「环节」「页面」是否混指同一层）
- **中文单字色名 / 类别名是否容易并作一类**：中文的「红」「紫」都是单字，
  直观上比英文的 Red / Purple 更易混淆。这类选项可以中英并列写在一个单元格里
  （`红（Red）`），比另开一列干净

## 升级与维护

改进候选、能力边界、以及**怎么提升级需求**，见 `references/roadmap.md`。

最关键的一条：提需求时**描述你遇到的摩擦**（"改完 md 重新生成，WPS 调的
行高又没了"），不要指定实现方案（"给我加个增量更新函数"）。
前者留出解法空间，后者会限制判断。
